# Plugin Example

Root-level maoloader plugin example for testing GitHub repository submissions.

The repository root `maoloader.json` points at this folder so the registry can fetch, validate, and mirror this repo as a plugin package.
